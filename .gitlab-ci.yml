#打包和发布一起执行
image: node:8.10.0

build:
  cache:
    paths:
    - node_modules/
  script:
  - npm install
  - npm run build
  - mkdir public
  - mv dist/* public
  artifacts:
    paths:
    - public
  only:
  - master